# 方案 A 实施记录

- 20260915 用户选择 A；验收优先级：整体大小，其次外侧留白。
- 最大宽度 920px，窄视口宽度为视口减 112px；靠右 40px，默认顶部 8vh，高度 80vh，底部约 12vh。
- 正文内边距 18px / 24px / 24px，标题与页脚左右 24px；无面板缩放补偿。
- 修复 `#xft-panel *` 的 margin/padding 重置压过组件类选择器的问题：组件规则统一置于面板 ID 之下。
- 保留全部业务模块、统计口径和导出逻辑。改动仅涉及 UI 样式、筛选日期容器、页脚按钮容器、定义浮层定位及拖动的底部边界。
- 版本 v0.1.9；manifest 和 package 同步，标题继续读取 manifest。

## 验证

- build.sh --all：19 个单测通过。
- 130 单虚构样例，真实 core.js / content.js / panel.css 本地渲染；无需登录或读取平台。
- CSS 视口 975×800：面板 863×640，左 72px / 右 40px / 底 96px。
- CSS 视口 1150×800、1300×800：面板 920×640，右 40px / 底 96px。
- CSS 视口 1676×960：面板 920×768，右 40px / 底约 115px。
- 四档正文 scrollWidth 未超过 clientWidth；26px 输入框、28px 按钮；末张 KPI 浮层位于面板内；维度切换正常。
- 生产页面刷新后的观感仍待用户截图验收；本地渲染不替代真实站点全局样式检验。
