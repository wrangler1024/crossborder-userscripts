# 悬浮护盾按钮

v0.1.15：右下角入口改用已定稿履约护盾，48×48px白底圆角按钮，32px图标，青色边框与浅青悬停效果。面板展开时隐藏入口，收起后显示，支持原生按钮键盘操作。

图像数据内嵌自icons/icon128.png，不增加资源授权或远程请求；图标后续更换时须同步此内嵌资产。

验证：23个测试通过；本地实际图片naturalWidth=128，按钮48×48；收起后入口可见，Enter展开面板并隐藏入口。未修改业务口径，未push或发布。
