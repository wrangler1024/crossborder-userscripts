# 采购详情首版开发交接

日期：20260914。状态：开发候选，未部署、未分发。

插件基线为 `10d1137`，配对执行器基线为 `6e42c37`，两个候选分支均为 `codex/purchase-details-v1`。

候选吸收 0.8.2 测试扩展的美国地址、CURP 和桌面数据源功能；正式候选版本 0.9.0，源码、包版本及运行验收分开记录。旧独立执行器、原始业务资料和配置未归并。

新增 `src/purchase-details.js` 维护凭证页签、一次确认、长图预览、结果查询、补图和修订交互。background 按标签页保存短时任务标识、白名单过滤消息，仅转发已允许操作。金额与截图由执行器读取，不接受网页内任意图片 URL。

共同协议详见主仓 `docs/生态跨仓契约.md` 的采购凭证候选章节。旧执行器未声明能力时隐藏写入操作；美国页面的采购详情读取被明确拒绝。

后续验收：隔离 HubStudio 环境安装、云端迁移、授权任务分配姓名/ID核对、单笔协作表实际写入与截图打开、断网后结果核对、Windows 执行器。测试完成前不得宣称上线。

本候选插件测试 47 项通过，构建成功。实际 Chromium 加载内容脚本、以脱敏页面拦截全部网络后验证订单详情页挂载、任务选择、订单金额展示，未出现 pageerror。云端和本机专项及限制见主仓对应交接。

## 20260914 动态等待提示

候选0.9.1：读取、截图、提交、补图和查询展示旋转动画、操作名称及实际等待秒数，不模拟后端完成百分比。异常与结束停止计时，回传异常仅允许查询结果。待发布；配套截图修复执行器0.17.23。
