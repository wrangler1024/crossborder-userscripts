# Xynigo 插件协作规约

版本：v1 草案 · 20260910。适用于 Codex、Cursor、ZCode 及其他参与者。开发、维护、评审角色按任务分配；草案提交供联合评审，不代表插件分发授权。

## 每单六条

1. **确认组件与版本。** 核对仓库、分支、HEAD、未提交改动、任务交接和受影响组件；分别确认源码版本、油猴分发版本、扩展包版本及实测状态，不把 README 或历史包当作当前运行证据。
2. **只改权威源。** 先查 [生成方向表](CONTRIBUTING.md#源码与生成方向)，修改源文件并更新受影响产物；不直接修改生成油猴脚本或 dist。协议、字段和链接格式变化须核对主仓共同契约及受影响插件。
3. **隔离并行工作。** 一个可独立交付的改动单元使用一个分支，默认前缀 `codex/`；并行任务用独立 worktree。同一工作区只有一个写入者；真实浏览器环境、共享库和发布目录另行协调。不能覆盖或清理他人的未提交改动。
4. **先验证再评审。** 开发者自查、自测，提交确切 SHA、diff 和验证结果；合并或发布前由未参与实现的一方完成最终评审。关闭“必须改”，确认受影响组件的必需检查实际运行且通过；现有 CI 未覆盖的组件须提供本地验证证据。
5. **把自动更新当分发。** 修改用户跟随的油猴更新路径、发布 tag/Release、上传团队扩展或替换下载包都需在明确授权范围内执行。真实写操作须经授权测试账号与小批量验收后才开放对应分发；预览、确认、精确匹配和结果未知保护不得省略。已有授权不逐步重问，扩大范围时说明新增影响。
6. **维护必要记录。** 状态变化更新任务交接，实践变化更新对应文档；契约变化更新主仓权威正文及本仓摘要/指针。重大源码归属、权限或分发策略调整由 Jeff 确认；不因每次普通代码修改重复修改规约。

## 安全与生态边界

- 测试使用合成/脱敏数据；不得将凭证、Cookie、真实接码链接、服务器地址、内部链接、原始订单/收件信息或完整 DOM 写入公开源码、测试、日志、PR 和资产。保持最小站点匹配与权限，不为方便调试扩大生产权限。
- 插件正式源码由本仓维护；私有业务空间的交付包按交付时点解释。若遗留源码更新，先核对并登记差异，不直接覆盖或宣称已归并。
- 主执行器与云端位于 [xynigo-sourcing](https://github.com/wrangler1024/xynigo-sourcing)。共同规则入口为其 [AGENTS.md](https://github.com/wrangler1024/xynigo-sourcing/blob/main/AGENTS.md)，字段和兼容权威为 [生态跨仓契约](https://github.com/wrangler1024/xynigo-sourcing/blob/main/docs/生态跨仓契约.md)。主仓契约权威不表示本仓继承其全部 Python/部署规则。
- 当前跨仓关系：XYP2、云端采购与登录、本机采购助手 API、精准采购链接；物流助手当前使用手工/文件输入，直接接入云端物流和幂等审计仍属待设计能力。
- 需要共同契约时，优先读取对应候选提交或已确认版本，不根据固定 `../` 路径寻找邻库。草案尚未合并时按交接中的配对分支读取；不能把尚未发布的 main 链接当成已经可访问。邻库不可用时可继续无关本地工作，但不能凭猜测修改跨仓接口。

## 查阅入口

开发、测试、生成检查和分发流程见 [CONTRIBUTING.md](CONTRIBUTING.md)；具体组件按其 README/INSTALL 和测试目录执行。需要共同评审、交接模板时读取主仓 [协作与交接](https://github.com/wrangler1024/xynigo-sourcing/blob/main/docs/协作与交接.md)。

根目录未跟踪的本地记忆不作为克隆必备依赖。现有组件交接中的“最新”仍须按核验时间复查。三端是否自动加载本文件需在新会话中验证；本草案不宣称已完成客户端配置或加载验收。
